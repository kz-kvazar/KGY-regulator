buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Здесь вы можете добавить зависимости для вашего buildscript, если это необходимо
        // Например, classpath 'com.android.tools.build:gradle:8.0.2'
    }
}

plugins {
    id 'com.android.application' version '8.0.2' apply false
    id 'com.android.library' version '8.0.2' apply false
}

tasks.register('clean', Delete) {
    delete rootProject.buildDir
}
